<!doctype html>
<html lang="{{ site.locale }}">

<head>
  {% render "head", metadata: site.metadata, title: page.title %}
</head>

<body class="bg-gray-600">
  <main>
    {{ content }}
  </main>
</body>

</html>